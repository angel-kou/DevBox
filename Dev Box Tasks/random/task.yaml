# yaml-language-server: $schema=../../task.yaml.schema.json

name: randomm
command: ""
inputs:
  stringInput:
      type: string
      required: true
      validationRegex: ^(0|[1-9][0-9]*)$
      minLength: 3
      maxLength: 10
  stringInputWithAllowedValues:
      type: string
      allowedValues: [ "ok" ]
  booleanInput:
      type: boolean
  numberInput:
      type: number
      
      
